apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service-app
  labels:
    app: payment-service-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service-app
  template:
    metadata:
      name: payment-service-app
      labels:
        app: payment-service-app
    spec:
      containers:
        - name: payment-service-app
          image: konped/payment-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
              protocol: TCP
          env:
            - name: CONFIG_SERVER_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: cloud-gateway-cm
                  key: CONFIG_SERVER_INSTANCE_HOSTNAME
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: mysql-cm
                  key: hostname
      restartPolicy: Always
      